//
// Created by wiseberg on 1/6/19.
//

#ifndef UNTITLED3_SLICEDECORATIR_H
#define UNTITLED3_SLICEDECORATIR_H


#include <cstddef>
#include "IDna.h"

class SliceDecorator : public IDna {
//
//public:
//
//    SliceDecorator(IDna * idna, std::size_t from, std::size_t to, std::size_t length): m_idna(idna), m_from(from), m_to(to);
//    std::size_t getLength();
//    const Necleotid &operator[](std::size_t index) const;
//
//private:
//    IDna * m_idna;
//    std::size_t m_from;
//    std::size_t m_to;
//    std::size_t m_length;
//
//
//};
//
//inline std::size_t SliceDecorator::getLength()
//{
//    return m_length;
//}
//
//const Necleotid & SliceDecorator::operator[](std::size_t index) const
//{
//    return (* m_idna)m_idna[index + m_from];
//}
};
#endif //UNTITLED3_SLICEDECORATIR_H
